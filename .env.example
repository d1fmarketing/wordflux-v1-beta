# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_REALTIME_MODEL=gpt-realtime

# AWS
AWS_REGION=us-west-2
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# DynamoDB
DYNAMO_TABLE=Wordflux
BOARD_PK=board#default

# Planka (Kanban externo)
PLANKA_BASE_URL=http://localhost:3015
# PLANKA_TOKEN=planka_api_token_aqui
# PLANKA_BOARD_ID=studio-board-id-ou-slug
# Para usar Planka como base do board
# BOARD_ADAPTER=planka
# Opções de robustez do AdapterPlanka
# (Default: usa fallback para o board interno se a chamada falhar)
# PLANKA_FALLBACK_INTERNAL=1
# Para forçar erro em vez de fallback, ative modo estrito:
# PLANKA_STRICT=1
# (Opcional) Ajuste de endpoints/campos caso sua versão do Planka use nomes diferentes
# PLANKA_API_PREFIX=/api
# PLANKA_ENDPOINT_BOARD=/api/boards/{boardId}
# PLANKA_ENDPOINT_LISTS=/api/boards/{boardId}/lists
# PLANKA_ENDPOINT_CARDS=/api/boards/{boardId}/cards
# PLANKA_ENDPOINT_CREATE_CARD=/api/cards
# PLANKA_ENDPOINT_UPDATE_CARD=/api/cards/{cardId}
# PLANKA_ENDPOINT_MOVE_CARD=/api/cards/{cardId}/move
# PLANKA_ENDPOINT_DELETE_CARD=/api/cards/{cardId}
# PLANKA_ENDPOINT_COMMENT_CARD=/api/cards/{cardId}/comments
# PLANKA_ENDPOINT_UPDATE_LIST=/api/lists/{listId}
# PLANKA_FIELD_CARD_TITLE=title
# PLANKA_FIELD_CARD_DESC=description
# PLANKA_FIELD_CARD_LIST_REF=listId
# PLANKA_FIELD_LIST_NAME=name
# (Opcional) Retentativas do AdapterPlanka
# PLANKA_RETRY_MAX=2
# PLANKA_RETRY_BASE_MS=250
# (Opcional) Cache do getBoard (ms)
# PLANKA_GETBOARD_TTL_MS=1000
# (Opcional) Timeout de fetch do AdapterPlanka (ms)
# PLANKA_FETCH_TIMEOUT_MS=8000

# Para embutir o Planka no frontend (iframe)
# Em produção (HTTPS), prefira usar o proxy interno: NEXT_PUBLIC_PLANKA_BASE_URL=/planka
NEXT_PUBLIC_PLANKA_BASE_URL=http://localhost:3015
# ID do board para o Chat/Agente (opcional, recomendado)
# NEXT_PUBLIC_PLANKA_BOARD_ID=studio-board-id-ou-slug
# Para forçar o grid interno (sem iframe):
# NEXT_PUBLIC_FORCE_GRID=1

# Local dev helpers (optional)
# Force in-memory board (bypass AWS):
# TEST_INMEMORY=1
# Return stubbed responses in /api/chat (no OpenAI key needed):
# ENABLE_TEST_ENDPOINTS=1
# Light UI (keep chat + board, hide extras):
# NEXT_PUBLIC_LITE_UI=1
# Minimal UI (hide chat + extras):
# NEXT_PUBLIC_MINIMAL_UI=1
