# Archived Documentation

**Date**: 2025-09-21
**Reason**: Outdated references (Claude as primary agent, port 3001, heuristic paths)
**Migration**: GPT-5 as primary agent, MCP-only mode, port 3000

## Archived Files
- `docker-mcp-gateway.md` - Referenced Claude Desktop as primary integration

## Migration Notes
- All active documentation updated to reflect GPT-5 as the runtime agent
- Port references changed from 3001 to 3000
- Removed heuristic/deterministic fallback documentation
- MCP-only mode is now enforced throughout the system

## Current Configuration
- **Agent**: GPT-5 Thinking (`gpt-5-thinking`)
- **Mode**: MCP-only (no fallbacks)
- **API Port**: 3000
- **UI Theme**: Quiet Kanban

---
*These files are preserved for historical reference and git history.*